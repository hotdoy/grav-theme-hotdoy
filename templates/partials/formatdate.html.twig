{% set lg = grav.language.getActive %}

{% if dateformat == 'short' %}
	{{ date|date('Y-m-d') }}

{% elseif dateformat == 'long' and lg == 'en' %}
	{{ date|date('F dS Y') }}

{% elseif dateformat == 'long' and lg == 'fr' %}
	{{ date|date('d') }} {{ 'GRAV.MONTHS_OF_THE_YEAR'|ta(post.date|date('n') - 1) }} {{ date|date('Y') }}
{% endif %}

