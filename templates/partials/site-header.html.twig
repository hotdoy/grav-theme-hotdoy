<header class="header site">
	
	{# Logo #}
    <a class="header__logo landscape" href="{{ base_url == '' ? '/' : base_url }}">Hotdoy</a>
    <a class="header__logo portrait" href="{{ base_url == '' ? '/' : base_url }}">Hotdoy</a>

    {# nav desktop #}
    <nav class="header__nav landscape">
		{% include '/partials/navigation.html.twig' %}
    </nav>

    {# nav mobile #}
    <div class="mobile-nav-open portrait">Menu</div>
   	<nav class="mobile-nav-panel portrait">
        <div class="material-icons mobile-nav-close">close</div>
		{% include '/partials/navigation.html.twig' %}
    </nav>

</header>
