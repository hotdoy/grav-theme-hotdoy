title: Grid
'@extends': modular/module

form:
  fields:
    tabs:
      fields:
        content:
          fields:

            content:
                unset@: true

            grid:
              type: section
              title: Grid

            header.rythm:
              type: toggle
              label: Rythm
              highlight: rythm
              default: rythm
              options: 
                rythm-negative: 'Negative'
                rythm-null: 'Null'
                rythm-half: 'Half'
                rythm: 'Default'

            header.columns:
              type: range
              label: Columns
              default: 3
              validate:
                min: 1
                max: 12

            header.bg_color:
              type: colorpicker
              label: Background Color

            items:
              type: section
              title: Items

            header.item_border:
              type: text
              label: Border

            header.item_padding:
              type: text
              label: Padding

            header.item_bg_color:
              type: colorpicker
              label: Background Color

            header.item_fg_color:
              type: colorpicker
              label: Foreground Color

            header.item_bg_color_alt:
              type: colorpicker
              label: Alternate Background Color

            header.item_fg_color_alt:
              type: colorpicker
              label: Alternate Foreground Color

            collection:
              type: section
              title: Collection

            header.content.items:
              type: text
              label: PLUGIN_ADMIN.ITEMS
              default: '@self.modular'
              size: medium

            header.content.order.by:
              type: text
              label: PLUGIN_ADMIN.ORDER_BY
              default: 'default'
              size: small

            header.content.order.dir:
              type: text
              label: PLUGIN_ADMIN.ORDER
              help: '"desc" or "asc" are valid values'
              default: 'asc'
              size: small
